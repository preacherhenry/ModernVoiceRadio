/**
 * The version of the Terms & Conditions currently in force.
 *
 * Stored against each account at registration so a future revision can be identified as
 * un-accepted without inferring it from dates. Bump this whenever the terms change
 * materially — accounts carrying an older version can then be asked to re-accept.
 */
export const CURRENT_TERMS_VERSION = '1.0';

export default CURRENT_TERMS_VERSION;
